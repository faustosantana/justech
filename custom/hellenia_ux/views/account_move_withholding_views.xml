<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_hellenia_withholdings" model="ir.ui.view">
        <field name="name">account.move.form.hellenia.withholdings</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="before">
                <group string="Retenciones"
                       invisible="move_type not in ('in_invoice', 'in_refund', 'out_invoice', 'out_refund') or state != 'draft'">
                    <group>
                        <field name="hellenia_ret_isr_gov" string="Gobierno 5%"
                               invisible="move_type not in ('out_invoice', 'out_refund')"/>
                        <field name="hellenia_ret_itbis_30" string="ITBIS retenido 30%"
                               invisible="move_type not in ('in_invoice', 'in_refund')"/>
                    </group>
                    <group>
                        <field name="hellenia_ret_isr_10" string="Proveedor informal 10%"
                               invisible="move_type not in ('in_invoice', 'in_refund')"/>
                        <field name="hellenia_ret_itbis_75" string="ITBIS informal 75%"
                               invisible="move_type not in ('in_invoice', 'in_refund')"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
