<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Multi-company: console is global; modules catalog is global -->
    <record id="rule_jac_audit_company" model="ir.rule">
        <field name="name">Justech Admin Audit: multi-company aware</field>
        <field name="model_id" ref="model_justech_admin_audit_log"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_justech_admin_center_user'))]"/>
    </record>
</odoo>
