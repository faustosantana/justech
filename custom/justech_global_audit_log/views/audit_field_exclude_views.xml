<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_justech_audit_field_exclude_list" model="ir.ui.view">
        <field name="name">justech.audit.field.exclude.list</field>
        <field name="model">justech.audit.field.exclude</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="field_name"/>
                <field name="model_id"/>
                <field name="is_sensitive"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_justech_audit_field_exclude" model="ir.actions.act_window">
        <field name="name">Campos excluidos</field>
        <field name="res_model">justech.audit.field.exclude</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
