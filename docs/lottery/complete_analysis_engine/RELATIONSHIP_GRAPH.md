# Relationship Graph

Directed, auditable graph. Node/edge types in `schemas.py`.  
`graph.complete` must be True before evidence/discovery.  
Each edge stores source, target, relation_type, table_type, observed_origin, depth, evidence_id, engine_version, table_version.
