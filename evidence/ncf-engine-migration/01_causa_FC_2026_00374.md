# FC/2026/00374 — causa raíz

| Campo | Valor |
|---|---|
| Factura | FC/2026/00374 (id 3716) |
| Empresa | JUSTECH S.R.L. |
| Tipo | out_invoice / posted |
| Tipo Justech | B01 - Factura de Crédito Fiscal |
| Motor invocado | **Justech** |
| Método | `justech.do.ncf.assignment.service.assign_before_post` |
| Resultado | Sin NCF |
| Rango consultado | vacío (no llegó a `_find_active_range`) |

## Causa raíz (única)

Motor Justech **sí** se ejecutó al publicar. `should_auto_assign_ncf` devolvió False porque `journal.justech_do_use_ncf=False`, y el código (pre-hardening) hizo `continue` silencioso. Además no existían rangos `justech.do.ncf.range`. Adel estaba congelado (`l10n_latam_use_documents=False` + freeze).

No fue: motor legacy, dual engine, ni fallo de feature flag (`ncf_motor=True`, `justech_do_fiscal_enabled=True`).

## Fase 10

Documento conservado como evidencia. **No** se asignó NCF retroactivo.
