# Helpdesk Enterprise config (DEV)

Reproducible configuration for Odoo Enterprise Helpdesk on `justech_dev`.

- No new models, crons, or mail systems
- Uses native auto-close cron and rating email template
- Improves existing automation #15 into a single digest email

See `evidence/helpdesk-enterprise-config-dev/00_RESUMEN.md`.
